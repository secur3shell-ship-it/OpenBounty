/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { OPENBOUNTY_V2_PROGRAM_ADDRESS } from "../programs";

export const VOTE_WINNER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  44, 247, 50, 25, 60, 91, 248, 84,
]);

export function getVoteWinnerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VOTE_WINNER_DISCRIMINATOR);
}

export type VoteWinnerInstruction<
  TProgram extends string = typeof OPENBOUNTY_V2_PROGRAM_ADDRESS,
  TAccountJudge extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountJudge extends string
        ? ReadonlySignerAccount<TAccountJudge> &
            AccountSignerMeta<TAccountJudge>
        : TAccountJudge,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      ...TRemainingAccounts,
    ]
  >;

export type VoteWinnerInstructionData = {
  discriminator: ReadonlyUint8Array;
  tierIndex: number;
  candidate: Address;
};

export type VoteWinnerInstructionDataArgs = {
  tierIndex: number;
  candidate: Address;
};

export function getVoteWinnerInstructionDataEncoder(): FixedSizeEncoder<VoteWinnerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tierIndex", getU8Encoder()],
      ["candidate", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: VOTE_WINNER_DISCRIMINATOR }),
  );
}

export function getVoteWinnerInstructionDataDecoder(): FixedSizeDecoder<VoteWinnerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tierIndex", getU8Decoder()],
    ["candidate", getAddressDecoder()],
  ]);
}

export function getVoteWinnerInstructionDataCodec(): FixedSizeCodec<
  VoteWinnerInstructionDataArgs,
  VoteWinnerInstructionData
> {
  return combineCodec(
    getVoteWinnerInstructionDataEncoder(),
    getVoteWinnerInstructionDataDecoder(),
  );
}

export type VoteWinnerInput<
  TAccountJudge extends InstructionSignerInput = InstructionSignerInput,
  TAccountEscrow extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** Must be one of `escrow.judges`. Each judge signs their own vote. */
  judge: TAccountJudge;
  escrow: TAccountEscrow;
  tierIndex: VoteWinnerInstructionDataArgs["tierIndex"];
  candidate: VoteWinnerInstructionDataArgs["candidate"];
};

export function getVoteWinnerInstruction<
  TAccountJudge extends InstructionSignerInput,
  TAccountEscrow extends InstructionAccountInput,
  TProgramAddress extends Address = typeof OPENBOUNTY_V2_PROGRAM_ADDRESS,
>(
  input: VoteWinnerInput<TAccountJudge, TAccountEscrow>,
  config?: { programAddress?: TProgramAddress },
): VoteWinnerInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountJudge,
    InstructionAccountInputAddress<TAccountJudge>
  >,
  ResolvedInstructionAccountMeta<
    TAccountEscrow,
    InstructionAccountInputAddress<TAccountEscrow>
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPENBOUNTY_V2_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    judge: { value: input.judge ?? null, isSigner: true, isWritable: false },
    escrow: { value: input.escrow ?? null, isSigner: false, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  return Object.freeze({
    accounts: [
      getAccountMeta("judge", accounts.judge),
      getAccountMeta("escrow", accounts.escrow),
    ],
    data: getVoteWinnerInstructionDataEncoder().encode(
      args as VoteWinnerInstructionDataArgs,
    ),
    programAddress,
  } as VoteWinnerInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountJudge,
      InstructionAccountInputAddress<TAccountJudge>
    >,
    ResolvedInstructionAccountMeta<
      TAccountEscrow,
      InstructionAccountInputAddress<TAccountEscrow>
    >
  >);
}

export type ParsedVoteWinnerInstruction<
  TProgram extends string = typeof OPENBOUNTY_V2_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Must be one of `escrow.judges`. Each judge signs their own vote. */
    judge: TAccountMetas[0];
    escrow: TAccountMetas[1];
  };
  data: VoteWinnerInstructionData;
};

export function parseVoteWinnerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVoteWinnerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { judge: getNextAccount(), escrow: getNextAccount() },
    data: getVoteWinnerInstructionDataDecoder().decode(instruction.data),
  };
}
